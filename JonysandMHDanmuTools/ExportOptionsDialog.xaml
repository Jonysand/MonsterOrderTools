<Window x:Class="MonsterOrderWindows.ExportOptionsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="导出打卡记录选项" Height="400" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" Text="选择导出格式：" FontSize="14" Margin="0,0,0,5"/>
        <ComboBox Grid.Row="1" Name="FormatComboBox" Width="200" HorizontalAlignment="Left" Margin="0,0,0,15">
            <ComboBoxItem Content="CSV" IsSelected="True"/>
            <ComboBoxItem Content="JSON"/>
        </ComboBox>
        
        <TextBlock Grid.Row="2" Text="筛选用户昵称（可选）：" FontSize="14" Margin="0,0,0,5"/>
        <TextBox Grid.Row="3" Name="UsernameTextBox" Width="200" HorizontalAlignment="Left" Margin="0,0,0,15"/>
        
        <TextBlock Grid.Row="4" Text="日期范围（可选）：" FontSize="14" Margin="0,0,0,5"/>
        <StackPanel Grid.Row="5" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="开始：" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <DatePicker Name="StartDatePicker" Width="120" Margin="0,0,15,0"/>
            <TextBlock Text="结束：" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <DatePicker Name="EndDatePicker" Width="120"/>
        </StackPanel>
        
        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="确定" Width="80" Height="30" Margin="0,0,10,0" Click="OkButton_Click"/>
            <Button Content="取消" Width="80" Height="30" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>